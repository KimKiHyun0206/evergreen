- name: Insert Decorated Daily Date
        run: |
          # 한국 시간 기준 날짜 및 요일 구하기 (예: 2025-12-22 Mon)
          TODAY=$(date -u -d "+9 hours" "+%Y-%m-%d (%a)")
          
          if [ ! -f daily-log.md ]; then
            echo "# 🌳 My Daily Learning Log" > daily-log.md
            echo -e "하루의 성장을 기록합니다.\n" >> daily-log.md
          fi

          # 1. 예쁘게 꾸며진 날짜 헤더 생성
          # > ### 📅 **2025-12-22 (Mon)** 형태
          echo "> ### 📅 **$TODAY**" > temp_entry.md
          echo "- " >> temp_entry.md  # 바로 내용을 적을 수 있게 불렛 포인트 추가
          echo -e "\n" >> temp_entry.md # 한 줄 띄우기
          
          # 2. 기존 내용을 뒤에 붙이기
          cat daily-log.md >> temp_entry.md
          mv temp_entry.md daily-log.md
